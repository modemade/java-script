/* const leInput = document.querySelector('input')
leInput.addEventListener('focus',function(){
    leInput.style.backgroundColor = "red"
})
leInput.addEventListener('blur',function(){
    leInput.style.backgroundColor = "rgba(255,255,255)"
})

const hover = document.querySelector('input:hover')
hover.addEventListener('focus',function(){
    hover.style.backgroundimage = url('./img/250581')
}) */

/* const lesImages= document.querySelectorAll('img')
console.log(lesImages);
const tabImg =Array.from(lesImages);
console.log(tabImg);
tabImg.map(function(uneImage,index){
    uneImage.addEventListener('load',function(){
        console.log(`image numéro : ${index}`);
    })
}) */
/* 
const text= document.querySelector('textarea')
const btn= document.querySelector('btn')
text.addEventListener('keyup',function(){
    console.log(text.value.length)
    if((text.value.length)>5 ){
        btn.setAttribute('disabled',"text");
    }

})
 */
